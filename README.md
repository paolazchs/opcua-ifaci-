# opcua-iot

Example repository combining an OPC UA server, Node-RED flows, an API, and a Next.js frontend for IoT device management.

Folders:
- `opcua-server` — Python OPC UA server
- `nodered` — Node-RED flows and settings
- `api` — Node API server
- `frontend` — Next.js frontend

Docker Compose is provided for local orchestration.
